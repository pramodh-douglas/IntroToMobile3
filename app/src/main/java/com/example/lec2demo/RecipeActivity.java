package com.example.lec2demo;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class RecipeActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_recipe);

        //Practice Exercise. Review the slides to see how the
        //recipe activity should look.
        //In your xml recipe layout, Add txtViewTitle2,
        //txtViewIngredTitle, txtViewIngred1
        //etc...txtViewDirecnTitle, txtViewDirecns etc.
        //set up id, Text and constraints, bias, margins etc.

    }
}